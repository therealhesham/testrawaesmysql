# دليل النشر السريع - نظام إدارة الشكاوى

## 📋 خطوات النشر

### 1. تحديث قاعدة البيانات

نموذج الشكاوى موجود بالفعل في `schema.prisma`. إذا لم تكن قد قمت بتطبيق التغييرات بعد:

```bash
# تطبيق التغييرات على قاعدة البيانات
npx prisma db push

# أو إنشاء migration جديد
npx prisma migrate dev --name add_complaints_system
```

### 2. إضافة الصلاحيات للأدوار الموجودة

قم بتشغيل السكريبت التالي لإضافة صلاحيات الشكاوى تلقائياً:

```bash
node scripts/add-complaints-permissions.js
```

**أو يمكنك إضافة الصلاحيات يدوياً:**

1. اذهب إلى صفحة إدارة الصلاحيات في النظام
2. اختر الدور الذي تريد منحه صلاحية حل الشكاوى (مثل: IT, دعم فني)
3. أضف الصلاحية التالية:

```json
{
  "إدارة الشكاوى": {
    "عرض": true,
    "حل": true
  }
}
```

### 3. التحقق من رفع الصور

تأكد من أن API رفع الصور (`/api/upload-image`) يعمل بشكل صحيح:

```bash
# تحقق من وجود المسار
ls pages/api/upload-image.ts
```

إذا لم يكن موجوداً، ستحتاج إلى إنشائه أو استخدام خدمة رفع صور خارجية.

### 4. إعادة تشغيل التطبيق

```bash
# في بيئة التطوير
npm run dev

# في بيئة الإنتاج
npm run build
npm start
```

## ✅ التحقق من التثبيت

### 1. للمستخدمين العاديين:
- [ ] اذهب إلى `/admin/personal_page`
- [ ] تأكد من ظهور تبويب "الشكاوى"
- [ ] جرب إرسال شكوى جديدة
- [ ] تحقق من ظهور الشكوى في القائمة

### 2. لمستخدمي IT:
- [ ] اذهب إلى `/admin/complaints`
- [ ] تأكد من ظهور جميع الشكاوى
- [ ] جرب تعديل حالة شكوى
- [ ] تحقق من وصول الإشعار للمستخدم

### 3. الإشعارات:
- [ ] أرسل شكوى جديدة
- [ ] تحقق من وصول إشعار لمستخدمي IT
- [ ] غيّر حالة الشكوى
- [ ] تحقق من وصول إشعار لصاحب الشكوى

## 🔧 استكشاف الأخطاء

### المشكلة: لا تظهر صفحة الشكاوى

**الحل:**
```bash
# تأكد من وجود الملفات
ls pages/admin/complaints/index.tsx
ls pages/api/complaints/index.ts
ls pages/api/complaints/[id].ts
```

### المشكلة: خطأ في رفع الصور

**الحل:**
- تحقق من وجود `/api/upload-image`
- تحقق من حجم الصورة (أقل من 10 ميجابايت)
- تحقق من صيغة الصورة (jpg, png, gif, webp)

### المشكلة: لا تظهر الصلاحيات

**الحل:**
```bash
# أعد تشغيل سكريبت الصلاحيات
node scripts/add-complaints-permissions.js

# أو أضف الصلاحيات يدوياً من واجهة الإدارة
```

### المشكلة: الإشعارات لا تعمل

**الحل:**
- تحقق من أن جدول `notifications` موجود في قاعدة البيانات
- تحقق من أن المستخدم لديه صلاحية "إدارة الشكاوى" > "حل"
- تحقق من console.log في المتصفح للأخطاء

## 📊 قاعدة البيانات

### جدول Complaint

```sql
-- التحقق من وجود الجدول
SELECT * FROM Complaint LIMIT 5;

-- عرض الإحصائيات
SELECT status, COUNT(*) as count 
FROM Complaint 
GROUP BY status;

-- عرض الشكاوى الحديثة
SELECT id, title, status, createdAt 
FROM Complaint 
ORDER BY createdAt DESC 
LIMIT 10;
```

## 🔐 الصلاحيات المطلوبة

### للمستخدمين العاديين:
- لا يحتاجون صلاحيات خاصة
- يمكنهم إرسال شكاوى ورؤية شكاواهم فقط

### لمستخدمي IT:
```json
{
  "إدارة الشكاوى": {
    "عرض": true,
    "حل": true
  }
}
```

## 📱 الوصول للصفحات

| الصفحة | المسار | الصلاحية المطلوبة |
|--------|--------|-------------------|
| شكاوى المستخدم | `/admin/personal_page` (تبويب الشكاوى) | جميع المستخدمين |
| إدارة الشكاوى | `/admin/complaints` | إدارة الشكاوى > حل |

## 🎯 الخطوات التالية

بعد النشر الناجح:

1. ✅ أنشئ شكوى تجريبية للتأكد من عمل النظام
2. ✅ تحقق من وصول الإشعارات
3. ✅ جرب تغيير حالة الشكوى
4. ✅ تأكد من عمل البحث والفلترة
5. ✅ اختبر رفع الصور

## 📞 الدعم

إذا واجهت أي مشاكل:

1. راجع ملف `COMPLAINTS_SYSTEM_README.md` للتفاصيل الكاملة
2. تحقق من console.log في المتصفح
3. تحقق من logs الخادم
4. تحقق من قاعدة البيانات

---

**ملاحظة مهمة:** تأكد من عمل نسخة احتياطية من قاعدة البيانات قبل تطبيق أي تغييرات!

```bash
# نسخة احتياطية سريعة
npx prisma db pull
```

✨ **مبروك! نظام الشكاوى جاهز للاستخدام!** ✨

