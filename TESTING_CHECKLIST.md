# قائمة اختبار نظام الشكاوى - Testing Checklist

## ✅ اختبارات المستخدم العادي

### 1. إرسال شكوى جديدة
- [ ] الوصول إلى `/admin/personal_page`
- [ ] الضغط على تبويب "الشكاوى"
- [ ] الضغط على "إرسال شكوى جديدة"
- [ ] إدخال عنوان الشكوى
- [ ] إدخال وصف الشكوى
- [ ] (اختياري) رفع صورة
- [ ] الضغط على "إرسال الشكوى"
- [ ] التحقق من ظهور رسالة نجاح
- [ ] التحقق من ظهور الشكوى في القائمة

### 2. عرض الشكاوى
- [ ] التحقق من ظهور جميع شكاوى المستخدم
- [ ] التحقق من ظهور الإحصائيات الصحيحة
- [ ] التحقق من ظهور الحالة الصحيحة لكل شكوى
- [ ] التحقق من ظهور تاريخ الإرسال

### 3. رفع الصور
- [ ] رفع صورة بصيغة JPG
- [ ] رفع صورة بصيغة PNG
- [ ] محاولة رفع ملف غير صورة (يجب أن يفشل)
- [ ] محاولة رفع صورة أكبر من 10 ميجابايت (يجب أن يفشل)
- [ ] التحقق من معاينة الصورة قبل الإرسال
- [ ] التحقق من إمكانية حذف الصورة قبل الإرسال

### 4. متابعة الشكوى
- [ ] التحقق من تحديث الحالة عند تغييرها من IT
- [ ] التحقق من ظهور المسؤول المُسند إليه
- [ ] التحقق من ظهور ملاحظات الحل
- [ ] التحقق من ظهور تاريخ الحل

## ✅ اختبارات مستخدم IT

### 1. الوصول إلى صفحة الإدارة
- [ ] الوصول إلى `/admin/complaints`
- [ ] التحقق من ظهور جميع الشكاوى
- [ ] التحقق من ظهور الإحصائيات الصحيحة
- [ ] التحقق من عدم إمكانية الوصول للمستخدمين بدون صلاحية

### 2. البحث والفلترة
- [ ] البحث بالعنوان
- [ ] البحث بالوصف
- [ ] البحث باسم المستخدم
- [ ] الفلترة حسب "قيد الانتظار"
- [ ] الفلترة حسب "قيد المعالجة"
- [ ] الفلترة حسب "تم الحل"
- [ ] الفلترة حسب "مغلقة"
- [ ] الفلترة حسب "جميع الحالات"

### 3. عرض تفاصيل الشكوى
- [ ] الضغط على أيقونة العرض (👁️)
- [ ] التحقق من ظهور جميع التفاصيل
- [ ] التحقق من ظهور معلومات المستخدم المُرسل
- [ ] التحقق من ظهور الصورة المرفقة (إن وجدت)
- [ ] التحقق من ظهور ملاحظات الحل (إن وجدت)

### 4. تعديل الشكوى
- [ ] الضغط على أيقونة التعديل (✏️)
- [ ] تغيير الحالة إلى "قيد المعالجة"
- [ ] حفظ التغييرات
- [ ] التحقق من تحديث الحالة
- [ ] تغيير الحالة إلى "تم الحل"
- [ ] إدخال ملاحظات الحل
- [ ] حفظ التغييرات
- [ ] التحقق من ظهور رسالة نجاح

### 5. الأولويات
- [ ] التحقق من ظهور خط أحمر للشكاوى الأقدم من 7 أيام
- [ ] التحقق من ظهور خط برتقالي للشكاوى الأقدم من 3 أيام
- [ ] التحقق من ظهور خط أزرق للشكاوى الجديدة

## ✅ اختبارات الإشعارات

### 1. إشعارات IT
- [ ] إرسال شكوى جديدة من مستخدم عادي
- [ ] التحقق من وصول إشعار لمستخدمي IT
- [ ] التحقق من محتوى الإشعار
- [ ] التحقق من نوع الإشعار (complaint)

### 2. إشعارات المستخدم
- [ ] تغيير حالة شكوى من IT
- [ ] التحقق من وصول إشعار للمستخدم صاحب الشكوى
- [ ] التحقق من محتوى الإشعار
- [ ] التحقق من نوع الإشعار (complaint_update)

## ✅ اختبارات الصلاحيات

### 1. صلاحيات العرض
- [ ] مستخدم عادي يرى شكاواه فقط
- [ ] مستخدم IT يرى جميع الشكاوى
- [ ] مستخدم بدون صلاحية لا يمكنه الوصول لصفحة الإدارة

### 2. صلاحيات التعديل
- [ ] مستخدم عادي لا يمكنه تعديل حالة الشكوى
- [ ] مستخدم IT يمكنه تعديل حالة الشكوى
- [ ] مستخدم IT يمكنه إضافة ملاحظات الحل

### 3. صلاحيات الحذف
- [ ] صاحب الشكوى يمكنه حذف شكواه
- [ ] مستخدم IT يمكنه حذف أي شكوى
- [ ] مستخدم آخر لا يمكنه حذف شكوى ليست له

## ✅ اختبارات API

### 1. GET `/api/complaints`
```bash
# جلب جميع الشكاوى
curl -X GET http://localhost:3000/api/complaints \
  -H "Cookie: authToken=YOUR_TOKEN"

# جلب شكاوى المستخدم فقط
curl -X GET "http://localhost:3000/api/complaints?myComplaints=true" \
  -H "Cookie: authToken=YOUR_TOKEN"

# فلترة حسب الحالة
curl -X GET "http://localhost:3000/api/complaints?status=pending" \
  -H "Cookie: authToken=YOUR_TOKEN"
```

### 2. POST `/api/complaints`
```bash
curl -X POST http://localhost:3000/api/complaints \
  -H "Content-Type: application/json" \
  -H "Cookie: authToken=YOUR_TOKEN" \
  -d '{
    "title": "شكوى تجريبية",
    "description": "هذه شكوى للاختبار",
    "screenshot": null
  }'
```

### 3. PUT `/api/complaints`
```bash
curl -X PUT http://localhost:3000/api/complaints \
  -H "Content-Type: application/json" \
  -H "Cookie: authToken=YOUR_TOKEN" \
  -d '{
    "id": 1,
    "status": "resolved",
    "resolutionNotes": "تم حل المشكلة بنجاح"
  }'
```

### 4. GET `/api/complaints/stats`
```bash
curl -X GET http://localhost:3000/api/complaints/stats \
  -H "Cookie: authToken=YOUR_TOKEN"
```

## ✅ اختبارات الأداء

### 1. سرعة التحميل
- [ ] صفحة الشكاوى تحمل في أقل من 2 ثانية
- [ ] الإحصائيات تظهر فوراً
- [ ] البحث يعمل بسرعة (أقل من 500ms)

### 2. التفاعل
- [ ] الأزرار تستجيب فوراً
- [ ] النماذج تفتح وتغلق بسلاسة
- [ ] الرسائل تظهر وتختفي بشكل صحيح

### 3. الذاكرة
- [ ] لا توجد تسريبات في الذاكرة
- [ ] الصور تُحمّل بكفاءة
- [ ] الإشعارات لا تتراكم

## ✅ اختبارات التوافق

### 1. المتصفحات
- [ ] Chrome
- [ ] Firefox
- [ ] Safari
- [ ] Edge

### 2. الأجهزة
- [ ] Desktop (1920x1080)
- [ ] Laptop (1366x768)
- [ ] Tablet (768x1024)
- [ ] Mobile (375x667)

### 3. الاتجاهات
- [ ] RTL (العربية) يعمل بشكل صحيح
- [ ] جميع النصوص محاذاة من اليمين
- [ ] الأيقونات في المكان الصحيح

## ✅ اختبارات الأمان

### 1. المصادقة
- [ ] المستخدم غير المسجل لا يمكنه الوصول
- [ ] انتهاء الجلسة يعيد التوجيه للتسجيل
- [ ] التوكن غير الصالح يُرفض

### 2. الصلاحيات
- [ ] المستخدم لا يرى شكاوى الآخرين
- [ ] المستخدم لا يمكنه تعديل شكاوى الآخرين
- [ ] API يتحقق من الصلاحيات في كل طلب

### 3. التحقق من البيانات
- [ ] العنوان الفارغ يُرفض
- [ ] الوصف الفارغ يُرفض
- [ ] الصورة غير الصالحة تُرفض
- [ ] الحجم الكبير يُرفض

## ✅ اختبارات قاعدة البيانات

### 1. الإدراج
```sql
-- التحقق من إدراج شكوى جديدة
SELECT * FROM Complaint ORDER BY createdAt DESC LIMIT 1;
```

### 2. التحديث
```sql
-- التحقق من تحديث الحالة
SELECT id, status, resolutionNotes, resolvedAt 
FROM Complaint 
WHERE id = 1;
```

### 3. العلاقات
```sql
-- التحقق من العلاقات
SELECT 
  c.id, 
  c.title, 
  u1.username as creator, 
  u2.username as assignedTo
FROM Complaint c
LEFT JOIN User u1 ON c.createdById = u1.id
LEFT JOIN User u2 ON c.assignedToId = u2.id;
```

## 📊 معايير النجاح

- ✅ جميع الاختبارات تمر بنجاح
- ✅ لا توجد أخطاء في console
- ✅ لا توجد تحذيرات في console
- ✅ الأداء ممتاز (< 2 ثانية للتحميل)
- ✅ التصميم متجاوب على جميع الأجهزة
- ✅ الإشعارات تعمل بشكل صحيح
- ✅ الصلاحيات محكمة

## 🐛 الإبلاغ عن الأخطاء

عند اكتشاف خطأ، قم بتسجيل:
1. خطوات إعادة إنتاج الخطأ
2. السلوك المتوقع
3. السلوك الفعلي
4. لقطة شاشة (إن أمكن)
5. رسالة الخطأ (إن وجدت)
6. المتصفح والجهاز

---

**ملاحظة:** يُفضل اختبار النظام في بيئة تطوير أولاً قبل النشر في الإنتاج.

✨ **اختبار سعيد!** ✨

